# 合同回款管理系统 需求说明书

| 项目     | 内容                       |
| -------- | -------------------------- |
| 项目名称 | 合同回款管理系统           |
| 文档版本 | V1.2                       |
| 编制日期 | 2026-05-11                 |
| 文档状态 | 草案（待评审）             |
| 适用范围 | 公司内部使用               |

## 修订记录

| 版本 | 日期       | 修订人 | 修订说明                                                                                              |
| ---- | ---------- | ------ | ----------------------------------------------------------------------------------------------------- |
| V1.0 | 2026-05-11 | -      | 初稿                                                                                                  |
| V1.1 | 2026-05-11 | -      | 闭环 Q2/Q3/Q4/Q7：取消"经理/主管"角色；合同编号采用推荐规则；客户不使用字典改为自由文本；部署由开发团队负责 |
| V1.2 | 2026-05-11 | -      | 闭环 Q1/Q5/Q6/Q8：实际回款登记与核销纳入本期；报表增加销售员维度；附件总容量 200GB 永久保留；管理员全量导出+审计与限频 |

---

## 1. 引言

### 1.1 编写目的

本文档用于明确"合同回款管理系统"的功能与非功能需求，作为产品设计、开发、测试和验收的统一依据，供项目相关方（业务、研发、测试、运维）共同评审与使用。

### 1.2 项目背景

公司目前的合同与回款信息通常分散在 Excel、邮件和个人电脑中，存在以下痛点：

- 合同台账不统一，关键信息易遗漏；
- 合同到期、回款节点缺乏主动提醒，容易延误；
- 应收账款进度无法实时统计，管理层难以全局掌握；
- 合同附件（扫描件、PDF）管理混乱，查找困难。

为此，公司决定建设一套内部使用的合同与回款管理系统，实现合同全生命周期记录、回款计划与到账登记、数据统计的线上化。

### 1.3 项目目标

- **集中管理**：合同台账与回款数据统一在线管理。
- **过程可控**：合同有效期、回款节点可视化、可提醒。
- **数据可见**：通过看板和趋势报表掌握整体回款情况。
- **可追溯**：所有关键操作均有日志，可审计。

### 1.4 术语与缩略语

| 术语     | 说明                                                 |
| -------- | ---------------------------------------------------- |
| 合同     | 公司与客户/供应商签订的具有法律效力的协议           |
| 回款计划 | 基于合同约定的分期收款安排                          |
| 实际回款 | 客户已支付到公司账户的款项                          |
| 应收余额 | 合同总金额 − 已实际回款金额                         |
| 账龄     | 应收款项自约定收款日起未收回的天数                  |
| RBAC     | 基于角色的访问控制（Role-Based Access Control）     |

---

## 2. 总体描述

### 2.1 用户角色

| 角色         | 主要职责                                                                                  |
| ------------ | ----------------------------------------------------------------------------------------- |
| 销售/业务员  | 录入与维护自己负责的合同；登记回款；查询自己的合同与回款数据。                            |
| 系统管理员   | 维护用户与角色、字典数据、系统参数；查看全局数据；导出全量数据；查看操作日志。            |

> **设计说明**：由于公司规模较小（<50 人），暂不设置审批流，所有合同录入即生效；权限模型仅区分"个人数据"与"全部数据"两档（销售看自己 + 管理员看全部）。

### 2.2 业务流程总览

```
[销售员] 录入合同 ──► 系统自动生成回款计划 ──► [销售员] 登记实际回款
                                                       │
                                                       ▼
                                    [系统] 自动核销、更新应收余额、生成提醒
                                                       │
                                                       ▼
                                         [所有人] 查看看板与趋势报表
```

### 2.3 假设与约束

- 用户均在公司内网或通过 VPN 访问，浏览器使用 Chrome/Edge 最新版本。
- 合同附件单文件不超过 50MB，单合同附件数量不超过 20 个。
- 货币默认为人民币（CNY），暂不支持多币种。
- 系统不与 ERP/CRM/银行系统对接，所有数据由用户手工录入。

### 2.4 部署形态

- **纯 Web 端**，浏览器访问，无移动端 App，不做小程序适配（页面在移动浏览器上可读但不优化交互）。

---

## 3. 功能需求

### 3.1 功能模块清单

```
合同回款管理系统
├── 1. 登录与个人中心
├── 2. 合同管理
│   ├── 2.1 合同录入
│   ├── 2.2 合同列表与检索
│   ├── 2.3 合同详情
│   ├── 2.4 合同附件管理
│   ├── 2.5 合同状态与到期提醒
│   └── 2.6 合同编辑/作废
├── 3. 回款管理
│   ├── 3.1 回款计划（自动生成 + 手工调整）
│   ├── 3.2 实际回款登记
│   ├── 3.3 回款核销与应收余额
│   └── 3.4 回款列表与检索
├── 4. 统计与报表
│   ├── 4.1 时间维度趋势报表
│   └── 4.2 销售员维度汇总报表
├── 5. 系统管理
│   ├── 5.1 用户管理
│   ├── 5.2 角色与权限
│   ├── 5.3 系统参数（合同编号规则、提醒规则）
│   └── 5.4 操作日志（审计）
```

---

### 3.2 合同管理

#### 3.2.1 合同录入

**功能说明**：销售员通过表单录入新合同，保存后立即生效（无审批）。

**字段清单**：

| 字段名       | 类型     | 必填 | 说明                                              |
| ------------ | -------- | ---- | ------------------------------------------------- |
| 合同编号     | 文本     | 是   | 唯一，建议系统按规则自动生成（如 HT-2026-0001）   |
| 合同名称     | 文本     | 是   | 最多 100 字                                       |
| 客户名称     | 文本     | 是   | 自由文本录入，最多 100 字（不使用客户字典）       |
| 合同金额     | 金额     | 是   | 含税金额，单位元，支持两位小数                    |
| 币种         | 下拉     | 是   | 默认 CNY                                          |
| 签订日期     | 日期     | 是   |                                                   |
| 生效日期     | 日期     | 是   | 默认等于签订日期，可改                            |
| 到期日期     | 日期     | 是   | 用于到期提醒                                      |
| 负责人       | 人员选择 | 是   | 默认为当前登录销售员                              |
| 合同附件     | 文件     | 否   | 可上传多个文件，单文件 ≤ 50MB                     |
| 备注         | 长文本   | 否   | 最多 500 字                                       |

**业务规则**：

- **合同编号由系统自动生成**，规则为 `HT-YYYY-XXXX`（4 位年内流水，从 0001 起，每年重置）。前缀与流水位数可由管理员在"系统参数"中调整。
- 系统不强校验"同客户同名合同"，但合同编号全局唯一。
- 由于不使用客户字典，"客户名称"为自由文本，**系统将按相同字符串视为同一客户**用于检索与统计，请录入时保持名称一致。
- 合同保存后将自动跳转至"回款计划生成"步骤（详见 3.3.1）。

#### 3.2.2 合同列表与检索

**列表展示字段**：合同编号、合同名称、客户、金额、签订日期、到期日期、负责人、合同状态、已回款金额、应收余额。

**检索条件**：合同编号/名称模糊搜索、客户名称（模糊匹配）、负责人、签订日期区间、到期日期区间、合同状态。

**默认排序**：签订日期倒序。

**权限规则**：

- 销售员：仅看到自己负责的合同。
- 管理员：看到全部合同。

**导出**：列表支持按当前筛选条件导出 Excel（管理员可见此按钮）。

#### 3.2.3 合同详情

展示合同所有字段、附件列表（在线预览 + 下载）、回款计划、实际回款流水、操作日志（仅管理员可见）。

#### 3.2.4 合同附件管理

- 支持 PDF、Word、Excel、JPG、PNG。
- 在线预览：PDF 与图片支持浏览器直接预览；Office 文档点击下载。
- 单个附件支持删除（仅合同负责人与管理员），删除操作记入日志。

#### 3.2.5 合同状态

| 状态     | 触发条件                                                  |
| -------- | --------------------------------------------------------- |
| 执行中   | 合同保存后默认状态                                        |
| 已完成   | 应收余额为 0 且当前日期 ≥ 到期日期，由系统每日自动判定    |
| 已到期   | 当前日期 > 到期日期 且 应收余额 > 0                       |
| 已作废   | 由负责人或管理员手工作废（需填写作废原因）                |

**到期提醒**：

- 合同到期前 30/15/7/1 天，在系统首页"待办"中提醒负责人。
- 站内消息形式提醒，无邮件/短信（本期不做集成）。

#### 3.2.6 合同编辑与作废

- **编辑**：金额、签订日期允许修改前提是"尚未登记任何实际回款"；否则仅允许修改备注、附件、到期日期。所有修改记入操作日志。
- **作废**：作废后合同不可编辑，回款计划失效；已登记的实际回款仍保留并标记"对应合同已作废"。

---

### 3.3 回款管理

#### 3.3.1 回款计划

**功能说明**：合同保存后，系统引导用户生成回款计划，也可后续在合同详情中编辑。

**回款计划字段**：

| 字段名       | 类型 | 必填 | 说明                              |
| ------------ | ---- | ---- | --------------------------------- |
| 期次         | 数字 | 是   | 第 1 期、第 2 期……                |
| 计划回款日期 | 日期 | 是   |                                   |
| 计划金额     | 金额 | 是   | 所有期次之和必须等于合同金额      |
| 备注         | 文本 | 否   |                                   |

**生成方式**：

1. **手工录入**：用户逐期填写。
2. **按比例自动生成**（推荐）：输入分期数量与每期百分比，系统自动计算金额与默认日期（如均匀间隔 30 天）。

**校验规则**：

- 所有期次"计划金额"之和必须严格等于合同金额（误差 < 0.01 元）。
- 计划回款日期必须 ≥ 合同签订日期。

**修改规则**：

- 已有实际回款核销的期次不可删除，金额仅允许向上调整。
- 修改记入操作日志。

#### 3.3.2 实际回款登记

**功能说明**：销售员/管理员登记客户实际到账款项，并关联到合同（及可选关联到具体回款期次）。

**字段清单**：

| 字段名       | 类型     | 必填 | 说明                                                    |
| ------------ | -------- | ---- | ------------------------------------------------------- |
| 关联合同     | 选择     | 是   | 从有效合同中选择                                        |
| 关联期次     | 选择     | 否   | 可选关联到具体期次，不选则视为"未指定期次"用于后续核销 |
| 实际到账日期 | 日期     | 是   |                                                         |
| 实际金额     | 金额     | 是   | > 0                                                     |
| 付款方       | 文本     | 否   | 默认带入合同客户名称                                    |
| 流水号       | 文本     | 否   | 银行回单号                                              |
| 凭证附件     | 文件     | 否   | 银行回单截图/PDF                                        |
| 备注         | 长文本   | 否   |                                                         |

#### 3.3.3 回款核销与应收余额

**自动核销规则**：

- 登记实际回款且指定了期次时，自动写入该期次的"已回款金额"。
- 未指定期次时，按"计划回款日期升序"依次抵扣未结清期次。
- 合同的"已回款金额"= 所有实际回款金额之和。
- 合同的"应收余额"= 合同金额 − 已回款金额。

**期次状态**：未回款 / 部分回款 / 已结清。

#### 3.3.4 回款列表与检索

- 列表字段：到账日期、合同编号、客户、实际金额、流水号、登记人、登记时间。
- 检索条件：合同编号/名称、客户、到账日期区间、登记人。
- 权限同合同模块：销售员看自己合同下的回款，管理员看全部。

---

### 3.4 统计与报表

#### 3.4.1 时间维度趋势报表

**报表内容**：

1. **合同签订金额趋势**：按月/季/年分组，柱状图展示每个周期内新签合同金额与数量。
2. **实际回款金额趋势**：按月/季/年分组，折线图展示每个周期内实际回款总额。
3. **签订 vs 回款对比**：同一坐标系内对比签订金额与回款金额。

**筛选条件**：时间粒度（月/季/年）、时间范围、负责人（管理员可见此筛选）。

**导出**：支持导出 PNG 图片和 Excel 原始数据（管理员）。

#### 3.4.2 销售员维度汇总报表

**功能说明**：用于管理层考核与对比销售员的合同与回款表现。

**报表内容**：

- 表格 + 柱状图，列出每位销售员的：合同数量、合同总金额、已回款金额、应收余额、**回款率**（已回款 / 合同总金额）。
- 支持按"回款率"或"合同总金额"排序。

**筛选条件**：时间范围（按合同签订日期）、合同状态。

**权限规则**：

- 管理员：可看全部销售员。
- 销售员：仅看到自己一行数据（提供个人 KPI 视角）。

**导出**：支持导出 Excel（管理员）。

> 由于"客户名称"采用自由文本（未建客户字典），同一客户可能存在多种写法，本期**不提供"按客户维度"的排名/汇总报表**，以免数据失真。V2 引入客户字典后再行实现。

---

### 3.5 系统管理

#### 3.5.1 用户管理（管理员）

- 新增、编辑、停用用户；字段：姓名、登录账号、密码（首次随机生成，强制首次登录修改）、邮箱、所属角色。
- 密码策略：8 位以上，含数字与字母。
- 停用用户无法登录，但其历史数据保留。

#### 3.5.2 角色与权限

- 内置两种角色：**销售员**、**管理员**。
- 本期不开放自定义角色与权限。

#### 3.5.3 系统参数（管理员）

- 合同编号生成规则（前缀、流水位数、是否按年重置）。
- 到期提醒规则（提醒节点，默认 30/15/7/1 天）。
- 附件上传限制（单文件大小、单合同数量上限）。
- 数据导出限制（单管理员每日最大导出次数，默认 5 次）。

> 本期不建设"客户字典"。客户信息直接在合同上自由录入。

#### 3.5.4 操作日志（审计）

**记录范围**：

- 登录/登出
- 合同的新增、修改、作废
- 回款计划的新增、修改、删除
- 实际回款的新增、修改、删除
- 附件的上传、下载、删除
- 用户、角色、系统参数的变更

**字段**：操作时间、操作人、操作 IP、模块、操作类型、目标对象、变更前/后值（关键字段）。

**查询**：仅管理员可见，支持按时间、操作人、模块筛选；保留期限 ≥ 2 年。

#### 3.5.5 全量数据导出（管理员）

**功能说明**：管理员可一键导出系统数据，用于备份、对账或交接。

**导出对象**：

- 合同列表（含所有字段，按当前筛选条件或不筛选导全量）。
- 回款列表（含所有字段）。
- 操作日志（按时间范围导出）。

**格式**：Excel（.xlsx）。

**安全控制**：

- 每次导出**强制记入操作日志**，字段包含：操作人、操作 IP、操作时间、导出对象、记录条数、文件 MD5。
- **频率限制**：单管理员每日最多 5 次导出，可在系统参数中调整。
- 导出文件命名规则：`{对象}_{YYYYMMDD_HHmmss}_{操作人}.xlsx`。
- 大于 10 万行的数据导出走异步任务，完成后站内消息通知下载。

---

### 3.6 登录与个人中心

- 账号密码登录；登录失败 5 次锁定 10 分钟。
- 首页：待办提醒（到期合同、未结清回款）+ 我的合同/回款快捷入口。
- 个人中心：修改密码、查看自己的操作记录。

---

## 4. 非功能性需求

### 4.1 性能

- 在 50 并发用户、年合同量 1000 份的数据规模下：
  - 列表页响应时间 ≤ 2s（P95）；
  - 详情页响应时间 ≤ 1.5s（P95）；
  - 报表生成 ≤ 5s。

### 4.2 可用性

- 工作日 9:00–19:00 可用性 ≥ 99.5%。
- 单次系统故障恢复时间（RTO）≤ 2 小时。

### 4.3 安全

- HTTPS 全站传输加密。
- 密码使用 bcrypt/argon2 哈希存储，禁止明文。
- 附件存储采用私有访问，下载链接需鉴权与短时效签名。
- 所有 API 接口进行身份与权限校验，防止越权访问。
- 操作日志不可删除、不可修改。

### 4.4 兼容性

- Chrome ≥ 110、Edge ≥ 110；不强制支持 IE 与 Safari。
- 屏幕分辨率最低 1366×768。

### 4.5 数据备份与存储容量

- 数据库每日 02:00 全量备份，保留近 30 天。
- 附件存储采用独立的对象存储（推荐 MinIO），按周打包备份。
- **附件总容量**：初始规划 200GB（按"年合同 1000 份 × 平均 5 个附件 × 平均 5MB"估算可支撑约 8 年）。
- **保留期限**：合同附件**永久保留**（属法律凭证，不主动删除）；如确需清理由管理员人工操作并记入审计日志。
- **容量预警**：使用率达 80% 时系统首页向管理员发出告警，由开发团队按运维预案扩容。

### 4.6 国际化

- 仅支持简体中文。

---

## 5. 数据模型概览

```
┌────────────┐       ┌──────────────┐       ┌──────────────────┐
│    User    │ 1   * │   Contract   │ 1   * │  PaymentPlanItem │
│ (用户)     ├───────┤  (合同)      ├───────┤  (回款计划期次)  │
└────────────┘       └──────┬───────┘       └────────┬─────────┘
                            │ 1                       │ 1
                            │                         │
                            │ *                       │ *
                     ┌──────┴───────┐         ┌───────┴────────┐
                     │  Attachment  │         │  Payment       │
                     │  (合同附件)  │         │  (实际回款)    │
                     └──────────────┘         └────────────────┘

┌──────────────┐
│  AuditLog    │  (操作日志，关联到 User 与目标对象)
└──────────────┘
```

主要实体：

- **User**：用户与角色。
- **Contract**：合同主表（客户名称作为合同字段冗余存储，不抽出独立实体）。
- **Attachment**：合同附件。
- **PaymentPlanItem**：合同的回款计划期次。
- **Payment**：实际回款流水。
- **AuditLog**：操作日志。

> 说明：因不建客户字典，"客户名称"作为合同表的一个普通字段存在，按字符串相等聚合统计；后续如有需要可在 V2 抽出独立 Customer 实体并做历史数据合并。

---

## 6. 技术架构建议（参考）

> 该部分为推荐方案，最终以技术评审为准。

- **前端**：Vue 3 / React 18，UI 库 Element Plus 或 Ant Design，构建工具 Vite。
- **后端**：Spring Boot 3（Java 17）或 Node.js（NestJS），RESTful API。
- **数据库**：MySQL 8 / PostgreSQL 15。
- **文件存储**：本地磁盘（小规模）或 MinIO（推荐，便于备份与权限控制）。
- **部署**：单机 Docker Compose 即可，组件包含 web、api、db、minio、定时任务。
- **定时任务**：每日凌晨执行——合同状态刷新、到期提醒生成、数据备份。
- **部署归属**：服务器/部署环境由 **开发团队** 提供与运维，公司侧仅需提供浏览器与网络访问条件；开发团队负责上线、备份策略落地与基础运维。

---

## 7. 项目里程碑（一次性交付完整版本）

| 阶段       | 工期（建议）  | 交付物                                                |
| ---------- | ------------- | ----------------------------------------------------- |
| 需求评审   | 1 周          | 本文档定稿、原型图                                    |
| 设计       | 2 周          | UI 设计稿、技术方案、数据库设计                       |
| 开发       | 6–8 周        | 全部功能模块开发完成，单元测试覆盖核心逻辑            |
| 测试       | 2 周          | 系统测试、UAT 测试报告                                |
| 上线与培训 | 1 周          | 部署、用户手册、培训                                  |
| 试运行     | 2 周          | 收集问题、修复缺陷                                    |

合计约 **14–16 周**。

---

## 8. 待确认事项（已全部闭环）

| 编号 | 议题                                                  | 状态     | 结论                                                                                       |
| ---- | ----------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------ |
| Q1   | 实际回款登记与核销是否纳入本期？                       | ✅ 已确认 | **纳入本期**，作为回款管理核心功能（§3.3.2、§3.3.3）                                       |
| Q2   | 合同编号生成方式？                                    | ✅ 已确认 | 系统自动生成，规则 `HT-YYYY-XXXX`（按年重置流水）                                          |
| Q3   | 是否需要"经理/主管"中间角色？                          | ✅ 已确认 | 不设置，仅销售员 + 管理员两档                                                              |
| Q4   | 是否使用客户字典？                                    | ✅ 已确认 | 不建字典，客户名称为合同上的自由文本字段                                                   |
| Q5   | 报表维度？                                            | ✅ 已确认 | **时间维度 + 销售员维度**；本期不做按客户维度（待 V2 引入客户字典后再做）                  |
| Q6   | 附件存储容量与保留期限？                              | ✅ 已确认 | 总容量 **200GB**，**永久保留**，使用率 80% 告警，单文件 50MB / 单合同 20 个附件            |
| Q7   | 服务器/部署环境归属？                                 | ✅ 已确认 | 由开发团队提供与运维                                                                       |
| Q8   | 管理员全量数据导出？                                  | ✅ 已确认 | **提供**，但**强制审计日志 + 频率限制（默认 5 次/天/人）+ 大文件异步导出**（§3.5.5）       |

---

## 9. 附录

### 9.1 参考状态机

**合同状态机**：

```
[新建] ──保存──► [执行中] ──应收余额=0 且 已到期──► [已完成]
                    │
                    └──当前日期 > 到期日期 且 余额>0──► [已到期]
                    │
                    └──手工作废──► [已作废]
```

**回款期次状态机**：

```
[未回款] ──部分到账──► [部分回款] ──全部到账──► [已结清]
[未回款] ─────────────全部到账─────────────► [已结清]
```

### 9.2 关键术语索引

应收余额、回款计划、实际回款、核销、账龄、字典、审计日志。

---

**— 文档结束 —**
