# 合同回款管理系统 · 操作手册

> 版本：V1.0    最后更新：2026-05-12
>
> 读者：销售员、销售管理员（系统管理员）
>
> 与配套文档：[需求说明书](./需求说明书.md) / [技术规格说明书](./技术规格说明书.md) / [部署文档](./部署文档.md) / [后续迭代计划](./后续迭代计划.md)

---

## 目录

1. [系统概览](#1-系统概览)
2. [角色与权限](#2-角色与权限)
3. [快速上手（5 分钟跑完一笔业务）](#3-快速上手5-分钟跑完一笔业务)
4. [登录、改密、退出](#4-登录改密退出)
5. [首页驾驶舱](#5-首页驾驶舱)
6. [合同管理](#6-合同管理)
7. [回款管理](#7-回款管理)
8. [用户与权限管理（仅管理员）](#8-用户与权限管理仅管理员)
9. [系统参数（仅管理员）](#9-系统参数仅管理员)
10. [操作审计日志](#10-操作审计日志)
11. [业务规则速查](#11-业务规则速查)
12. [常见问题（FAQ）](#12-常见问题faq)
13. [名词表](#13-名词表)

> 标注 `〔截图位〕` 处的截图请由实施 / 培训人员补齐到 `docs/screenshots/` 后，把对应 Markdown 行替换为 `![说明](./docs/screenshots/xxx.png)`。

---

## 1. 系统概览

合同回款管理系统服务于销售团队，覆盖**合同登记 → 回款计划 → 实际回款核销 → 应收余额跟踪**的完整闭环：

- **合同管理**：录入、编辑、作废合同；系统自动生成 `HT-YYYY-NNNN` 编号
- **回款计划**：每份合同可生成 1 ~ 100 期的回款计划，支持等额、按比例、手工三种模式
- **实际回款**：登记到账金额，系统按期次顺序自动核销，实时刷新合同应收余额与状态
- **首页驾驶舱**：合同总数 / 总金额 / 已收 / 应收一目了然
- **基础设施**：操作均产生审计日志，所有附件存 MinIO，登录有失败次数锁定保护

---

## 2. 角色与权限

| 角色 | 标识 | 范围 |
| --- | --- | --- |
| **销售员** | `SALES` | 只能看见 / 操作 **自己作为负责人** 的合同；可登记任意合同的回款（受合同负责人限制）；不能管理用户与系统参数 |
| **销售管理员** | `ADMIN` | 全部合同的查看、编辑、作废；用户管理 / 系统参数 / 全部审计 |

### 权限矩阵

| 功能 | SALES（自己合同） | SALES（他人合同） | ADMIN |
| --- | --- | --- | --- |
| 查看首页驾驶舱 | ✅（按本人合同口径） | - | ✅（全公司口径） |
| 合同 新建 | ✅（负责人 = 自己） | - | ✅ |
| 合同 列表/详情 | ✅ | ❌ | ✅（看全部） |
| 合同 编辑 | ✅ | ❌ | ✅ |
| 合同 作废 | ✅ | ❌ | ✅ |
| 回款计划 生成 / 查看 | ✅ | ❌ | ✅ |
| 实际回款 登记 / 删除 | ✅ | ❌ | ✅ |
| 用户管理 | ❌ | ❌ | ✅ |
| 系统参数 | ❌ | ❌ | ✅ |
| 审计日志（个人） | ✅（仅本人） | - | ✅（全部） |

> "他人合同" 的权限通过后端 `SecurityHelper.assertOwnerOrAdmin()` 拦截，越权访问会返回 `20101 无权访问此资源`。

---

## 3. 快速上手（5 分钟跑完一笔业务）

> 假设你是销售员，账号已由管理员创建并告知初始密码。

1. 打开浏览器访问公司分配的地址（如 `https://contract.example.com`）
2. 用账号 + 初始密码登录 → 进入"修改密码"页 → 设置新密码 → 自动进首页
3. 顶部菜单 **合同管理 → 新建合同** → 填写合同信息（合同名称、客户、金额、签订/生效/到期日期、负责人选自己）→ **保存**
4. 系统跳到"回款计划"页 → 选**等额均分**、期数 3、间隔 30 天 → **生成** → 看到 3 期计划
5. 顶部菜单 **回款管理 → 登记回款** → 搜索刚才的合同号 → 选第 1 期 → 录入到账金额、到账日期 → **保存**
6. 回到 **合同管理 → 点开刚才的合同** → 看见"回款计划"列表里第 1 期状态变成"已结清"，进度条 100%；"实际回款流水"里多了一条记录
7. 顶部菜单 **首页驾驶舱** → 看到"已回款金额"已经增加

🎉 完成一笔最小闭环。

---

## 4. 登录、改密、退出

### 4.1 登录

`〔截图位〕登录页`

- 网址：`https://contract.example.com`
- 输入账号、密码，点 **登录**
- 登录态有效期 **2 小时**，超时后自动跳回登录页

#### 登录失败规则

- 密码错误会提示 **"用户名或密码错误"**
- 连续 **5 次** 失败，账号会被锁定 **10 分钟**，提示 **"账号已被锁定，请稍后再试"**
- 锁定期间正确密码也不能登录
- 如紧急解锁，联系系统管理员（管理员可在后台清除锁定，参见 [§12 FAQ](#12-常见问题faq)）

### 4.2 首次登录强制改密

`〔截图位〕改密页`

- 新建账号或管理员重置密码后，再次登录会**强制**进入"修改密码"页
- 旧密码：填初始密码（如 `Admin@1234` 或管理员告知的随机密码）
- 新密码：
  - 长度 **8 - 32 位**
  - **必须**包含字母 **和** 数字
  - 强烈建议同时包含大小写与符号
- 改密成功会自动跳进首页驾驶舱

### 4.3 日常修改密码

- 右上角头像下拉 → **修改密码**
- 输入旧密码 + 新密码（同样规则）→ 提交
- 提交成功后**不会**自动登出，但建议手动重新登录以确认

### 4.4 退出登录

- 右上角头像下拉 → **退出登录**
- 服务端会把当前 JWT 拉黑名单，即使有人复制了 token 也无法继续访问

---

## 5. 首页驾驶舱

`〔截图位〕首页驾驶舱`

进入系统的默认页，主要包含：

| 卡片 | 数据来源 | 备注 |
| --- | --- | --- |
| 合同总数 | 未作废合同 | SALES 只统计自己负责的；ADMIN 看全公司 |
| 合同总金额 | SUM(合同金额) | 同上 |
| 已回款金额 | SUM(实际回款金额) | 同上 |
| 应收余额 | 合同金额 - 已回款 | 同上 |
| 合同状态分布 | 按状态分组的条形 | 执行中 / 已完成 / 已到期 |

> 数字为**实时**计算（每次进入页面拉取最新）。若不刷新，按 F5 即可。

---

## 6. 合同管理

### 6.1 合同列表

`〔截图位〕合同列表`

顶部菜单 **合同管理** 进入列表，提供：

- **多条件筛选**：关键字（合同编号 / 名称）、客户、状态、签订日期范围
- **分页**：默认 20 条 / 页，可切到 50 / 100
- **列**：合同编号、合同名称、客户、合同金额、已回款、应收余额（应收 > 0 时高亮）、签订/到期日期、负责人、状态、操作

**操作**：

- **查看**：点 *查看* 进入合同详情
- **编辑**：仅未作废合同可编辑
- **新建合同**：页面右上角按钮

### 6.2 新建合同

`〔截图位〕新建合同表单`

字段说明：

| 字段 | 必填 | 校验 / 说明 |
| --- | --- | --- |
| 合同名称 | ✅ | 1-100 字符 |
| 客户名称 | ✅ | 1-100 字符，自由文本（不绑定客户字典） |
| 合同金额 | ✅ | > 0，最多 2 位小数（单位：元，币种默认 CNY） |
| 币种 | - | 默认 CNY，目前仅显示 |
| 签订日期 | ✅ | 日期选择器 |
| 生效日期 | ✅ | ≥ 签订日期 |
| 到期日期 | ✅ | > 生效日期 |
| 负责人 | ✅ | 下拉选择（启用用户列表）；SALES 默认自己 |
| 备注 | - | ≤ 500 字符 |

**保存后**：

1. 系统按 `HT-YYYY-NNNN` 规则生成合同编号（如 `HT-2026-0007`）
2. 状态默认 **执行中**
3. 已回款 / 应收余额初始为 0 / 合同金额
4. 自动跳到"回款计划"生成页（也可右上 *返回* 后再去）

### 6.3 合同详情

`〔截图位〕合同详情`

顶部三块：

1. **基本信息**：编号、名称、客户、金额、已回款、应收余额、签订 / 生效 / 到期日期、负责人、创建时间、备注
2. **回款计划**：每期次的"计划金额 / 已回款 / 进度 / 状态 / 备注"，状态分 *未回款 / 部分回款 / 已结清*
3. **实际回款流水**：所有真实到账记录，包含到账日期、金额、付款方、流水号、登记人、登记时间

**右上角按钮**：

- *编辑*：进入编辑页
- *作废*：弹窗输入作废原因，确认后状态变 **已作废**
- *返回*：返回列表

### 6.4 编辑合同

- 只能编辑：合同名称、客户、金额、生效 / 到期日期、负责人、备注
- **不可编辑**：合同编号、签订日期（关系到编号规则）、状态
- 编辑金额会导致回款计划与应收余额需要重新评估，建议谨慎；系统会给出二次确认弹窗

### 6.5 作废合同

`〔截图位〕作废弹窗`

- 点 *作废* → 弹窗强制输入 **作废原因（≤ 500 字符）**
- 已作废合同：
  - 不能再登记回款（接口直接拒绝）
  - 不能再编辑
  - 仍可查看详情和历史回款
- **作废不删数据**，审计日志记录全过程

### 6.6 合同状态机

| 状态 | 中文 | 进入条件 | 出口 |
| --- | --- | --- | --- |
| `EXECUTING` | 执行中 | 新建即此状态 | 完成（→ COMPLETED）、到期（→ EXPIRED）、作废（→ VOIDED） |
| `COMPLETED` | 已完成 | 已回款 ≥ 合同金额且未到期 | 作废（→ VOIDED） |
| `EXPIRED` | 已到期 | 到期日 < 当前日且未完成 | 作废（→ VOIDED） |
| `VOIDED` | 已作废 | 显式作废 | 终态 |

> 状态变化目前由"业务事件"驱动（登记回款、修改合同时评估）。计划在 [IT-1.3](./后续迭代计划.md) 上线每日定时扫描，自动把过期合同打成 `EXPIRED`。

---

## 7. 回款管理

### 7.1 生成回款计划

`〔截图位〕回款计划生成`

合同详情 → "回款计划" 区域点 *生成计划*（或新建合同保存后自动跳入）。

支持 **三种模式**：

| 模式 | 适用 | 必填参数 |
| --- | --- | --- |
| **UNIFORM 等额均分** | 简单分期，每期金额相同 | 期数（1-100）、间隔天数（默认 30）、首期日期（默认签订日 + 间隔） |
| **RATIO 按比例分期** | 首付 + 尾款等不均匀分期 | 期数、每期百分比列表（合计 = 100）、间隔天数、首期日期 |
| **MANUAL 手工录入** | 完全自定义 | 每期：期次号、计划日期、计划金额、备注 |

**校验规则**：

- RATIO 模式：`ratios.size == periods`，且总和 **必须等于 100**
- MANUAL 模式：期次号必须从 1 起递增；计划金额之和 ≤ 合同总金额（保留误差容忍 0.01）
- 已有计划的合同**不允许**重复生成（避免覆盖已核销的数据）；如需调整，先把现有计划清零（IT-1.2 后台支持），再重新生成

生成成功后：

- 期次按 `period_no` 排序展示
- 每期初始状态 = **未回款**，已回款金额 = 0

### 7.2 登记实际回款

`〔截图位〕登记回款`

顶部菜单 **回款管理 → 登记回款**：

1. **搜索合同**：输入编号 / 名称关键字，下拉选合同
2. **关联期次**（可选）：选具体期次会"定向核销"；不选则按未结清期次顺序自动抵扣
3. **录入到账信息**：
   - 到账日期（必填）
   - 到账金额（必填，> 0）
   - 付款方（选填，自由文本，便于对账）
   - 银行流水号（选填，建议填）
   - 备注（选填）
4. 点 **保存**

**核销算法（一句话总结）**：

> 系统在合同上加行锁（`SELECT ... FOR UPDATE`），按 *期次号正序* 把到账金额逐期抵扣未结清部分；抵扣到本期满 → 状态变 *已结清*，未满 → *部分回款*；全部抵扣完后剩余金额若 > 0 提示"金额超过合同应收余额"并整笔回滚。

**示例**：

合同总额 ¥30000，已生成 3 期等额 ¥10000，全部未回款。

- 第 1 笔登记 ¥8000：
  - 第 1 期：已回款 ¥8000，状态 *部分回款*
  - 第 2 / 3 期不动
  - 合同已回款：¥8000，应收余额：¥22000
- 第 2 笔登记 ¥15000：
  - 第 1 期补齐到 ¥10000，*已结清*
  - 第 2 期：已回款 ¥10000（吃完 ¥13000 还剩 ¥3000），*已结清*
  - 第 3 期：已回款 ¥3000，*部分回款*
  - 合同已回款：¥23000，应收余额：¥7000
- 第 3 笔登记 ¥7000：
  - 第 3 期补齐到 ¥10000，*已结清*
  - 合同状态：**已完成**（已回款 = 合同金额）

> 如果选了具体期次：只往该期抵扣，超过该期未结清金额会报错"金额超过本期未结清金额"。

### 7.3 回款列表

`〔截图位〕回款列表`

顶部菜单 **回款管理**：

- **筛选**：到账日期范围
- **列**：到账日期、合同编号、客户、金额、付款方、流水号、登记人、登记时间、操作
- **操作**：
  - *查看合同*：跳到对应合同详情
  - *删除*：弹窗确认，删除后自动**反向核销**（撤销该笔回款对所有期次的抵扣，重算合同已回款）

### 7.4 删除回款（反向核销）

- 仅 ADMIN 或合同负责人可操作
- 删除会**回滚抵扣**：
  - 减少各期次 `received_amount`
  - 重新评估各期次状态（满 → 部分 / 未回款）
  - 减少合同 `paid_amount`、增加 `remaining_amount`
  - 评估合同状态（COMPLETED 可能变回 EXECUTING）
- 删除有审计日志，无法物理"消失"（DBA 可在 `payment` 表 `deleted=1` 中追溯）

---

## 8. 用户与权限管理（仅管理员）

> 当前迭代后端接口已就绪（`/api/v1/users`），**前端管理页面**尚未上线（计划在 [IT-1.6](./后续迭代计划.md)）。在前端页面上线前，管理员可通过下述两种方式完成用户操作：

### 8.1 临时操作方式

**方式 A：Swagger 在线调用**

1. 仅在内网（或经 VPN）访问 `https://contract.example.com/swagger-ui.html`
2. 登录后端获取 JWT：点 *认证 → /api/v1/auth/login → Try it out*，请求体 `{"username":"admin","password":"<your_pwd>"}`
3. 复制返回的 `token`，点页面右上角 *Authorize* 输入 `Bearer <token>`
4. 进 **用户管理** 分组，按需调用增删改查

**方式 B：HTTP 客户端**

```bash
# 创建用户（管理员调用）
curl -X POST https://contract.example.com/api/v1/users \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"username":"zhangsan","name":"张三","email":"zhangsan@example.com","role":"SALES"}'
# 返回：{"username":"zhangsan","initialPassword":"<10位随机串>"}
```

### 8.2 业务操作清单

| 操作 | 接口 | 说明 |
| --- | --- | --- |
| 新建用户 | `POST /api/v1/users` | 自动生成 10 位随机密码，`must_change_pwd=1`，首次登录强制改密 |
| 编辑用户 | `PUT /api/v1/users/{id}` | 可改：姓名、邮箱、角色（**不可改用户名**） |
| 停用用户 | `POST /api/v1/users/{id}/disable` | 停用后无法登录；现有 JWT 仍有效，建议同时清除其 Redis 登录态 |
| 重置密码 | `POST /api/v1/users/{id}/reset-password` | 返回新随机密码，告知用户后他需首次改密 |
| 分页查询 | `GET /api/v1/users?page=&size=&keyword=` | 支持用户名 / 姓名模糊搜索 |

### 8.3 角色分配建议

- **新员工入职**：管理员创建 SALES 账号 → 通知本人首次密码与登录地址 → 本人首次登录改密
- **管理员变更**：仅维持 1-2 名 ADMIN，避免泛滥；离职前必须先用 `disable` 停用
- **离职 / 调岗**：先把负责的合同**转交**给同事（合同 → 编辑 → 更换负责人）再停用账号，避免数据无主

### 8.4 紧急解锁账号

用户连续 5 次输错被锁定，10 分钟内无法登录。管理员可立即解锁：

```bash
# 进入 Redis 容器
docker exec -it cm-redis-prod redis-cli -a $REDIS_PASSWORD
> DEL login:lock:<username>
> DEL login:fail:<username>
```

---

## 9. 系统参数（仅管理员）

> 与用户管理相同：后端接口（`/api/v1/sys-params`）已就绪，前端页面待 [IT-1.5](./后续迭代计划.md)。

### 9.1 可配置参数清单

| Key | 默认 | 说明 |
| --- | --- | --- |
| `contract.no.prefix` | `HT` | 合同编号前缀 |
| `contract.no.serial_length` | `4` | 流水位数（4 位 → 0001-9999） |
| `contract.no.reset_yearly` | `true` | 是否按年重置流水 |
| `contract.remind.days` | `30,15,7,1` | 合同到期前提醒节点（天，IT-1.3 启用） |
| `attachment.max_size_mb` | `50` | 单附件大小上限（MB） |
| `attachment.max_count_per_contract` | `20` | 单合同附件数量上限 |
| `export.daily_limit_per_admin` | `5` | 管理员每日导出次数上限（IT-3.1 启用） |
| `storage.alert_threshold_percent` | `80` | MinIO 存储水位告警阈值 |

### 9.2 操作

```bash
# 查询
curl https://contract.example.com/api/v1/sys-params -H "Authorization: Bearer $TOKEN"

# 修改
curl -X PUT https://contract.example.com/api/v1/sys-params/contract.no.prefix \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"value":"CT"}'
```

- 参数变更**会触发缓存刷新**（Redis），无需重启后端
- 改前缀 / 流水位数后**新合同**生效，已有合同编号不变

---

## 10. 操作审计日志

所有关键写操作（登录、改密、合同 CRUD、回款 CRUD、用户管理、参数修改）都会异步落库到 `audit_log` 表：

| 字段 | 含义 |
| --- | --- |
| `user_id` / `username` | 操作人 |
| `ip` | 客户端 IP |
| `module` | `AUTH / CONTRACT / PAYMENT / USER / SYS_PARAM ...` |
| `action` | `LOGIN / LOGOUT / CREATE / UPDATE / DELETE / VOID / RESET_PASSWORD ...` |
| `target_type` / `target_id` | 操作对象 |
| `before_json` / `after_json` | 变更前 / 后快照（DELETE 时仅有 before） |
| `result` | `SUCCESS / FAIL` |
| `error_msg` | 失败原因（如有） |
| `created_at` | 时间 |

> 前端查询页待 [IT-1.4](./后续迭代计划.md) 上线，目前可由 DBA 直接查询 `audit_log` 表。

**典型查询示例**：

```sql
-- 查张三今天做了什么
SELECT created_at, module, action, target_id, result
FROM audit_log
WHERE username='zhangsan' AND created_at >= CURRENT_DATE
ORDER BY id DESC;

-- 查某合同的所有变更
SELECT created_at, username, action, before_json, after_json
FROM audit_log
WHERE target_type='Contract' AND target_id='123'
ORDER BY id ASC;

-- 查失败登录
SELECT username, ip, created_at, error_msg
FROM audit_log
WHERE module='AUTH' AND action='LOGIN' AND result='FAIL'
ORDER BY id DESC LIMIT 100;
```

---

## 11. 业务规则速查

### 11.1 合同编号生成

- 格式：`<前缀>-<年份>-<流水>`，如 `HT-2026-0007`
- 实现：Redis `INCR` 原子自增；启动时与 DB 对账；按年重置（如启用）
- 跨日期 / 应用重启 / 多实例都不会重号

### 11.2 日期校验

| 规则 | 校验位置 |
| --- | --- |
| 生效日期 ≥ 签订日期 | 后端 + 前端 |
| 到期日期 > 生效日期 | 后端 + 前端 |
| 回款到账日期 ≥ 合同签订日 | 后端 |
| 回款到账日期 ≤ 当天 | 后端 |
| 回款计划首期日期 ≥ 签订日 | 后端 |

### 11.3 金额校验

- 货币精度：所有金额保留 **2 位小数**，DB 用 `DECIMAL(18,2)`
- 合同金额 > 0
- 单笔回款金额 > 0；累计回款 ≤ 合同金额（否则整笔回滚）
- 等额均分若除不尽，**末期自动吸收尾差**（保证总和 = 合同金额）
- 按比例分期：百分比之和必须恰好 = 100，最末期金额吸收尾差

### 11.4 密码策略

- 长度 8-32
- 必须包含字母 + 数字
- BCrypt cost=10 存储
- 重置 / 新建账号下发的随机密码：长度 10，字符集 `ABCDEFGHJKLMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz23456789`（去掉了易混字符 `Ii Ll Oo 1 0`）

### 11.5 登录保护

- 失败 5 次 → 锁 10 分钟
- 失败计数与锁定状态都存 Redis（key：`login:fail:<username>`、`login:lock:<username>`），TTL 自动过期
- 锁定期间正确密码也会被拒绝

### 11.6 JWT 与会话

- 有效期 2 小时
- 登出后该 token 进 Redis 黑名单（与原 TTL 一致），直到自然过期；之后即使被人捡到也不可用
- 不实现"踢人下线"，但停用账号后**新登录**会被拒，旧 token 仍可用直到过期或显式登出

---

## 12. 常见问题（FAQ）

**Q1. 登录提示"用户名或密码错误"，但我确认密码正确。**

- 检查输入法是否英文 / 大小写锁定
- 是否被锁定？继续错 1 次会看到锁定提示
- 联系管理员重置密码：`POST /api/v1/users/{id}/reset-password`

**Q2. 我是销售员，看不到同事的合同。**

- 这是设计行为。SALES 只能看见自己作为负责人的合同
- 如确实需要协作，请管理员把合同的负责人改成你，或临时把你升为 ADMIN

**Q3. 改密码后页面没反应。**

- 已修复（V1.0），如复现：1) Ctrl/Cmd + Shift + R 强制刷新；2) 退出后重新登录

**Q4. 登记回款时弹"金额超过合同应收余额"。**

- 合同剩余应收已不足以容纳本次到账
- 检查合同详情的应收余额；或核实是否本笔回款应录到另一个合同
- 如确实是合同金额错了，先去合同详情改金额，再来登记

**Q5. 误删了一笔回款。**

- 删除会反向核销，**业务数据可恢复**：让管理员或 DBA 在 `payment` 表把 `deleted=1` 的记录改回 `0`，并相应地把抵扣加回去（建议提工单走审计流程）
- 操作过程会留下 audit_log

**Q6. 合同号断了，比如从 0007 直接跳到 0010。**

- 极少见，可能原因：1) 创建合同时事务回滚（用户取消保存）；2) Redis 短暂故障重启
- 系统**不保证**编号绝对连续，只保证不重复；如业务对连续性强约束，请走 IT-1.7 之后的"号段管理"方案

**Q7. 附件上传 / 下载怎么用？**

- 当前 V1.0 后端附件链路尚未联通到前端（计划 [IT-1.1](./后续迭代计划.md)）
- 临时可用 Swagger 调 MinIO 直传，但不推荐生产使用

**Q8. 怎么导出合同 Excel？**

- 同步导出（小批量）：合同列表右上角"导出"按钮 → 选筛选条件 → 下载（待 [IT-3.1](./后续迭代计划.md)）
- 异步导出（大批量）：后台提交导出任务，完成后站内消息通知

**Q9. 报表 / 销售排行 / 客户分析在哪？**

- 在 [IT-1.2 报表模块](./后续迭代计划.md) 中实现，当前 V1.0 仅有首页驾驶舱

**Q10. 系统会自动把过期合同标为"已到期"吗？**

- 当前**不会**自动标记；状态在每次"登记回款 / 编辑合同"时评估一次
- 计划在 [IT-1.3](./后续迭代计划.md) 上线每日 02:00 定时扫描

**Q11. 系统支持手机端吗？**

- 前端已使用响应式栅格，**iPad 横屏**体验良好；手机竖屏可访问但表单较紧凑
- 不提供原生 App

---

## 13. 名词表

| 名词 | 含义 |
| --- | --- |
| **合同（Contract）** | 一份与客户签订的销售协议，含金额、签订/到期日期等 |
| **回款计划（Payment Plan）** | 合同未来若干期次的预期到账安排 |
| **期次（Period / Plan Item）** | 回款计划中的单期，含 *计划日期、计划金额、已回款、状态* |
| **实际回款（Payment）** | 客户真实到账的一笔款项 |
| **核销（Reconciliation）** | 把实际回款按期次顺序抵扣计划金额的过程 |
| **状态机** | 合同 / 期次的状态流转规则，详见 [§6.6](#66-合同状态机) |
| **审计日志（Audit Log）** | 关键操作的留痕，含操作人、IP、变更前后快照 |
| **JWT** | 服务端签发的会话令牌；2 小时过期；登出会拉黑名单 |
| **驾驶舱（Dashboard）** | 首页的关键数据汇总视图 |
| **附件** | 上传到 MinIO 的文件，按合同 / 回款关联 |

---

> 文档归口：产品 / 实施团队。
> 反馈与勘误：在 Issue Tracker 中提 issue，加标签 `docs/user-manual`。
> 培训：新员工入职 1 周内由部门负责人安排 30 分钟实操培训，覆盖本手册 §3 ~ §7。
