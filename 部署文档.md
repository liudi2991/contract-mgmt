# 合同回款管理系统 · 部署文档

> 版本：V1.0    最后更新：2026-05-12
>
> 适用范围：开发环境（一键 Docker）、测试环境（Docker / 主机混合）、生产环境（单机 Docker / 双机高可用）

---

## 目录

1. [系统架构](#1-系统架构)
2. [部署清单与环境要求](#2-部署清单与环境要求)
3. [部署拓扑](#3-部署拓扑)
4. [开发环境部署（10 分钟跑起来）](#4-开发环境部署10-分钟跑起来)
5. [测试 / 生产环境部署](#5-测试--生产环境部署)
6. [配置文件详解](#6-配置文件详解)
7. [初始化与首次登录](#7-初始化与首次登录)
8. [上线 Checklist](#8-上线-checklist)
9. [备份与恢复](#9-备份与恢复)
10. [升级与回滚](#10-升级与回滚)
11. [监控、日志与告警](#11-监控日志与告警)
12. [安全加固](#12-安全加固)
13. [性能调优](#13-性能调优)
14. [故障排查手册](#14-故障排查手册)
15. [附录：常用运维命令速查](#15-附录常用运维命令速查)

---

## 1. 系统架构

### 1.1 组件清单

| 组件 | 角色 | 技术栈 | 默认端口 |
| --- | --- | --- | --- |
| Web Frontend | SPA 单页应用 | Vue 3 + TypeScript + Vite + Element Plus | 5173（开发）/ 80（生产） |
| API Backend | 业务服务 | Spring Boot 3.2 + Java 17 + MyBatis-Plus | 8080 |
| MySQL | 主数据库 | MySQL 8.0 InnoDB / utf8mb4 | 3306 |
| Redis | 缓存 / 分布式锁 / JWT 黑名单 / 登录锁定 | Redis 7 | 6379 |
| MinIO | 附件对象存储 | MinIO（S3 兼容） | 9000 / 9001（控制台） |
| Nginx（生产） | 反向代理 + 静态资源 + HTTPS | Nginx 1.24+ | 80 / 443 |

### 1.2 调用关系

```
浏览器 ──HTTPS──▶ Nginx ──/api──▶ Backend ──▶ MySQL
                       │                  ├─▶ Redis
                       │                  └─▶ MinIO
                       └──/        ──▶ 前端静态文件
```

- 前端通过 `/api/*` 调用后端，由 Nginx 反向代理；本地开发由 Vite 代理 `5173 → 8080`
- 后端无状态（JWT），可水平扩容
- MySQL / Redis / MinIO 默认单机；如需高可用见 [§3.3](#33-生产高可用拓扑参考)

---

## 2. 部署清单与环境要求

### 2.1 服务器最低规格（生产单机，<1000 合同 / 月）

| 资源 | 最低 | 推荐 |
| --- | --- | --- |
| CPU | 2 vCore | 4 vCore |
| 内存 | 4 GB | 8 GB |
| 磁盘 | 50 GB SSD | 100 GB SSD（含日志 / 附件预留） |
| 带宽 | 10 Mbps | 50 Mbps |
| 操作系统 | CentOS 7+ / Ubuntu 20.04+ / Rocky 8+ | Ubuntu 22.04 LTS |

> 内存分配参考：MySQL 1.5G / Redis 256M / MinIO 256M / Backend JVM Xmx 2G / Nginx 128M / 系统预留 ≥ 1G。

### 2.2 软件依赖

| 软件 | 版本要求 | 用途 |
| --- | --- | --- |
| Docker | ≥ 24.0 | 容器化部署（强烈推荐） |
| Docker Compose | v2（CLI plugin） | 一键编排 |
| Java | OpenJDK 17 | 仅当不使用容器部署后端时 |
| Maven | 3.9.x | 编译后端 |
| Node | 20 LTS | 编译前端 |
| pnpm | 9.x | 前端包管理 |
| Nginx | 1.24+ | 生产反向代理 |

### 2.3 网络与端口

- **对外开放**：80、443（仅生产）
- **内网开放（仅在主机间互通时需要）**：3306（MySQL）、6379（Redis）、9000（MinIO API）、9001（MinIO 控制台，建议仅内网）、8080（后端 API，仅内网）
- 出方向：需要可访问公司邮件服务器（如启用邮件提醒）、NTP（time1.aliyun.com 等）

### 2.4 域名与证书（生产）

- 至少 1 个二级域名，例如 `contract.example.com`
- HTTPS 证书：Let's Encrypt 免费证书 / 公司 CA 颁发证书均可，覆盖期 ≥ 90 天

---

## 3. 部署拓扑

### 3.1 开发拓扑

单台开发机，Docker Compose 跑 MySQL / Redis / MinIO，后端 / 前端在 IDE 中本地启动。详见 [§4](#4-开发环境部署10-分钟跑起来)。

### 3.2 测试 / 生产单机拓扑（推荐起点）

```
┌─────────────────────────────────────────────┐
│ 单台 Linux 主机                              │
│                                             │
│  Nginx ─┐                                   │
│         ├──▶ contract-mgmt-backend (8080)   │
│         └──▶ /usr/share/nginx/html (前端)   │
│                                             │
│  MySQL  Redis  MinIO （全部 Docker 容器）    │
└─────────────────────────────────────────────┘
```

适用场景：日活 < 50 人、合同 < 10000 条、不需要高可用。

### 3.3 生产高可用拓扑（参考）

```
            ┌───────────┐     ┌───────────┐
浏览器 ─▶  │   SLB     │ ─▶  │ Nginx ×2  │ (Keepalived VIP)
            └───────────┘     └─────┬─────┘
                                    │
                       ┌────────────┼────────────┐
                       ▼            ▼            ▼
                 Backend ×N   Backend ×N   ... (无状态可水平扩展)
                       │            │
       ┌───────────────┼────────────┼───────────────┐
       ▼               ▼            ▼               ▼
   MySQL 主从      Redis 哨兵     MinIO 分布式      备份机
```

> 当前迭代未提供高可用脚本，需在 [后续迭代计划.md](./后续迭代计划.md) IT-4.5 之后补齐。

---

## 4. 开发环境部署（10 分钟跑起来）

### 4.1 前置准备

```bash
docker --version          # ≥ 24.0
docker compose version    # v2
java -version             # 17
mvn -v                    # 3.9+
node -v                   # 20
pnpm -v                   # 9
```

### 4.2 拉取代码与环境变量

```bash
git clone <repo-url> contract-mgmt
cd contract-mgmt
cp .env.example .env       # 必做：缺这个 docker compose 会报 "couldn't find env file"
```

### 4.3 拉起基础设施

```bash
docker compose -f docker-compose.dev.yml --env-file .env up -d
docker compose -f docker-compose.dev.yml ps   # 三个容器都应是 running
```

### 4.4 启动后端

```bash
cd backend
./mvnw spring-boot:run          # 首次会下载依赖，约 3-5 分钟
```

启动成功标志：

```
... ContractMgmtApplication : Started ContractMgmtApplication in 7.2 seconds
... [main] Tomcat started on port(s): 8080 (http)
```

打开 `http://localhost:8080/swagger-ui.html` 验证 API 可达。

### 4.5 启动前端

```bash
cd frontend
pnpm install
pnpm dev
```

打开 `http://localhost:5173`，使用 `admin / Admin@1234` 登录（首次会引导改密）。

---

## 5. 测试 / 生产环境部署

> 当前迭代尚未提供官方生产 Dockerfile（待 [IT-4.1](./后续迭代计划.md) 上线），下文给出**两种**可立即落地的生产部署方式：A. 直接运行 jar；B. 自行构建 Docker 镜像。

### 5.1 准备工作

```bash
# 1. 创建专用账号
sudo useradd -m -s /bin/bash contract
sudo usermod -aG docker contract

# 2. 创建目录结构
sudo mkdir -p /opt/contract-mgmt/{app,logs,backup,config}
sudo chown -R contract: /opt/contract-mgmt

# 3. 时区
sudo timedatectl set-timezone Asia/Shanghai
```

### 5.2 构建产物

**后端 jar：**

```bash
cd backend
./mvnw clean package -DskipTests
ls target/contract-mgmt-backend.jar    # 产物
```

**前端静态文件：**

```bash
cd frontend
pnpm install --frozen-lockfile
pnpm build
ls dist/                                # 部署到 Nginx
```

> 前端构建前需根据生产域名调整 API base URL。建议在 `vite.config.ts` 已通过 Nginx 同源代理后保持 `/api` 不变；如跨域，参考 [`src/utils/request.ts`](./frontend/src/utils/request.ts) 的 `baseURL` 注释。

### 5.3 方案 A：直接运行 jar（最简）

#### 5.3.1 部署基础设施（Docker）

复用开发版 compose，仅修改 `.env` 中的密码：

```bash
# /opt/contract-mgmt/config/.env  （务必修改默认密码！）
DB_ROOT_PASSWORD=<32 位随机串>
DB_PASSWORD=<同上>
REDIS_PASSWORD=<32 位随机串>
MINIO_ACCESS_KEY=<16 位随机串>
MINIO_SECRET_KEY=<32 位随机串>
MINIO_BUCKET=contract-mgmt
JWT_SECRET=<≥ 64 位随机串>
```

> 生成强随机密码：`openssl rand -base64 32` 或 `pwgen -s 32 1`

```bash
cd /opt/contract-mgmt
cp docker-compose.dev.yml docker-compose.prod.yml
# 编辑 prod 文件：去掉宿主端口绑定中 3306/6379/9001 三项（仅暴露 9000 给后端，所有依赖仅内网）
docker compose -f docker-compose.prod.yml --env-file config/.env up -d
```

**建议的 `docker-compose.prod.yml` 调整：**

- `mysql.ports` 改为 `127.0.0.1:3306:3306`
- `redis.ports` 改为 `127.0.0.1:6379:6379`
- `minio.ports` 改为 `127.0.0.1:9000:9000` 与 `127.0.0.1:9001:9001`（控制台仅本机访问）
- `mysql` 增加 `volumes: ['/opt/contract-mgmt/data/mysql:/var/lib/mysql']` 持久化到主机目录

#### 5.3.2 部署后端 jar

```bash
cp backend/target/contract-mgmt-backend.jar /opt/contract-mgmt/app/

# 启动脚本：/opt/contract-mgmt/app/start.sh
cat <<'EOF' | sudo tee /opt/contract-mgmt/app/start.sh
#!/bin/bash
set -euo pipefail
cd /opt/contract-mgmt/app
exec java \
  -server -Xms1g -Xmx2g \
  -XX:+UseG1GC -XX:MaxGCPauseMillis=200 \
  -XX:+HeapDumpOnOutOfMemoryError \
  -XX:HeapDumpPath=/opt/contract-mgmt/logs/heapdump.hprof \
  -Dspring.profiles.active=prod \
  -Dfile.encoding=UTF-8 \
  -Duser.timezone=Asia/Shanghai \
  --add-opens java.base/java.lang=ALL-UNNAMED \
  -jar contract-mgmt-backend.jar
EOF
chmod +x /opt/contract-mgmt/app/start.sh
```

创建 `application-prod.yml`（参考 [§6.3](#63-application-prodyml)）放到 `/opt/contract-mgmt/app/config/`，jar 同目录的 `config/` 会被 Spring Boot 自动加载。

#### 5.3.3 注册 systemd 服务

```bash
sudo tee /etc/systemd/system/contract-mgmt-backend.service <<'EOF'
[Unit]
Description=Contract Management Backend
After=docker.service network-online.target
Requires=docker.service

[Service]
Type=simple
User=contract
EnvironmentFile=/opt/contract-mgmt/config/.env
ExecStart=/opt/contract-mgmt/app/start.sh
SuccessExitStatus=143
Restart=always
RestartSec=10
StandardOutput=append:/opt/contract-mgmt/logs/backend.log
StandardError=append:/opt/contract-mgmt/logs/backend-error.log
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable --now contract-mgmt-backend
sudo systemctl status contract-mgmt-backend
```

#### 5.3.4 部署前端 + Nginx

```bash
sudo apt install -y nginx
sudo mkdir -p /var/www/contract-mgmt
sudo cp -r frontend/dist/* /var/www/contract-mgmt/
sudo chown -R www-data: /var/www/contract-mgmt
```

Nginx 站点配置 `/etc/nginx/sites-available/contract-mgmt.conf`：

```nginx
upstream contract_backend {
    server 127.0.0.1:8080;
    keepalive 32;
}

server {
    listen 80;
    server_name contract.example.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name contract.example.com;

    ssl_certificate     /etc/nginx/ssl/contract.example.com.crt;
    ssl_certificate_key /etc/nginx/ssl/contract.example.com.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
    ssl_session_cache   shared:SSL:10m;

    client_max_body_size 200m;       # 与后端 multipart.max-request-size 一致

    # 安全头
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=31536000" always;

    # 静态资源
    root /var/www/contract-mgmt;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;     # SPA 路由
        expires -1;
    }
    location /assets/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # API 代理
    location /api/ {
        proxy_pass         http://contract_backend;
        proxy_http_version 1.1;
        proxy_set_header   Connection "";
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_send_timeout   60s;
        proxy_read_timeout   60s;
    }

    # Swagger / Actuator 仅允许内网
    location ~ ^/(swagger-ui|v3/api-docs|actuator)/ {
        allow 10.0.0.0/8;
        allow 172.16.0.0/12;
        allow 192.168.0.0/16;
        deny  all;
        proxy_pass http://contract_backend;
    }

    access_log /var/log/nginx/contract-access.log;
    error_log  /var/log/nginx/contract-error.log warn;
}
```

```bash
sudo ln -s /etc/nginx/sites-available/contract-mgmt.conf /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
```

### 5.4 方案 B：后端容器化（推荐用于多机 / CI）

后端项目根目录新增 `Dockerfile`（待 [IT-4.1](./后续迭代计划.md)）：

```dockerfile
FROM eclipse-temurin:17-jre-jammy
RUN apt-get update && apt-get install -y --no-install-recommends fonts-noto-cjk \
    && rm -rf /var/lib/apt/lists/*
ENV TZ=Asia/Shanghai
WORKDIR /app
COPY target/contract-mgmt-backend.jar app.jar
EXPOSE 8080
ENTRYPOINT ["sh","-c","java $JAVA_OPTS -jar app.jar"]
```

```bash
cd backend
./mvnw clean package -DskipTests
docker build -t contract-mgmt-backend:1.0.0 .
```

在 `docker-compose.prod.yml` 中新增 backend service，环境变量从 `.env` 注入，业务库/Redis/MinIO 通过 Docker 网络互联即可。

---

## 6. 配置文件详解

### 6.1 `.env`

| Key | 必填 | 说明 | 生产建议 |
| --- | --- | --- | --- |
| `DB_ROOT_PASSWORD` | ✅ | MySQL root 密码 | 32 位随机串 |
| `DB_PASSWORD` | ✅ | 应用连接 MySQL 的密码（当前等同 root，建议生产分离） | 同上 |
| `REDIS_PASSWORD` | ✅ | Redis 鉴权密码 | 32 位随机串 |
| `MINIO_ACCESS_KEY` | ✅ | MinIO 访问密钥 | 16+ 位 |
| `MINIO_SECRET_KEY` | ✅ | MinIO 秘密密钥 | 32+ 位 |
| `MINIO_BUCKET` | ✅ | 业务桶名 | `contract-mgmt` |
| `JWT_SECRET` | ✅ | JWT 签名密钥 | **≥ 64 位**，否则后端启动失败 |

> 切勿把含真实密码的 `.env` 提交进 Git。仓库已加入 `.gitignore`，请勿移除。

### 6.2 `application.yml`（通用配置）

关键项位于 `backend/src/main/resources/application.yml`：

| 配置 | 默认 | 说明 |
| --- | --- | --- |
| `server.port` | 8080 | 后端端口 |
| `spring.servlet.multipart.max-file-size` | 60MB | 单文件大小上限（与 Nginx `client_max_body_size` 对齐） |
| `spring.servlet.multipart.max-request-size` | 200MB | 单请求大小上限 |
| `app.jwt.expire-seconds` | 7200 | JWT 过期时间（2 小时） |
| `app.minio.presigned-expire-seconds` | 900 | 附件预签名 URL 有效期 |
| `app.login.max-fail` | 5 | 登录失败上限 |
| `app.login.lock-minutes` | 10 | 锁定时长（分钟） |
| `mybatis-plus.global-config.db-config.logic-delete-*` | deleted = 1 | 软删除字段 |

### 6.3 `application-prod.yml`

新建 `/opt/contract-mgmt/app/config/application-prod.yml`（与 jar 同级 `config/` 目录下，Spring Boot 自动加载）：

```yaml
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/contract_mgmt?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: ${REDIS_PASSWORD}
      database: 0
      timeout: 5s
      lettuce:
        pool:
          max-active: 32
          max-idle: 16
          min-idle: 4

app:
  jwt:
    secret: ${JWT_SECRET}
    expire-seconds: 7200
  minio:
    endpoint: http://127.0.0.1:9000
    access-key: ${MINIO_ACCESS_KEY}
    secret-key: ${MINIO_SECRET_KEY}
    bucket: ${MINIO_BUCKET}

logging:
  level:
    root: INFO
    com.company.contract: INFO
    org.springframework.security: WARN
  file:
    name: /opt/contract-mgmt/logs/app.log
  logback:
    rollingpolicy:
      max-file-size: 100MB
      max-history: 30
      total-size-cap: 5GB

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, prometheus
  endpoint:
    health:
      show-details: never        # 生产仅返回 UP/DOWN
```

### 6.4 Docker Compose 持久化卷

| 容器 | 数据 | 卷 | 生产建议 |
| --- | --- | --- | --- |
| `cm-mysql` | DB | `/var/lib/mysql` | 绑定到主机目录 `/opt/contract-mgmt/data/mysql`，独立磁盘 / RAID |
| `cm-redis` | RDB / AOF | `/data` | 同上：`/opt/contract-mgmt/data/redis` |
| `cm-minio` | 对象 | `/data` | 同上：`/opt/contract-mgmt/data/minio` |

---

## 7. 初始化与首次登录

### 7.1 数据库初始化

启动后端时 Flyway 会自动执行 `V1__init_schema.sql`：

- 建 9 张业务表（`sys_user`、`contract`、`payment_plan_item`、`payment`、`attachment`、`audit_log`、`sys_param`、`export_record`、`notification`）
- 插入默认管理员：`admin / Admin@1234`（`must_change_pwd=1`）
- 插入 8 条系统参数（合同编号前缀、附件限制、提醒节点等）

校验：

```sql
USE contract_mgmt;
SHOW TABLES;                              -- 应有 9 张表 + flyway_schema_history
SELECT * FROM sys_user;                   -- 应有 admin 一条
SELECT * FROM sys_param;                  -- 应有 8 条
```

### 7.2 MinIO Bucket 自动创建

后端启动时 `MinioConfig` 会检查并创建 `${MINIO_BUCKET}`。若失败（如 MinIO 启动慢于后端），可手动登录控制台 `http://<host>:9001` 用 `MINIO_ACCESS_KEY/SECRET_KEY` 登录后创建。

### 7.3 首次登录强制改密

1. 浏览器打开 `https://contract.example.com`
2. 使用 `admin / Admin@1234` 登录
3. 系统自动跳转到改密页，输入旧密码 + 8-32 位且必须包含字母与数字的新密码
4. 改密成功跳到驾驶舱

> **生产强烈建议**：上线后立即改密，并将 `admin` 账号备用，业务操作走专门创建的 ADMIN / SALES 账号。

---

## 8. 上线 Checklist

部署完成后逐项核对：

### 8.1 基础设施

- [ ] `.env` 所有密码 / Secret 已改为 32+ 位随机串，未使用任何默认值
- [ ] `JWT_SECRET` ≥ 64 字符
- [ ] MySQL、Redis、MinIO 容器仅监听 `127.0.0.1` 或内网网卡，不对公网开放
- [ ] MinIO 控制台 9001 已通过 Nginx 限制 IP 白名单
- [ ] 服务器防火墙仅开放 80、443
- [ ] 时区为 `Asia/Shanghai`、NTP 已启用

### 8.2 应用

- [ ] `application-prod.yml` 已部署到 `/opt/contract-mgmt/app/config/`
- [ ] systemd 服务已 `enable`，`systemctl status` 显示 `active (running)`
- [ ] `curl -sf http://127.0.0.1:8080/actuator/health` 返回 `{"status":"UP"}`
- [ ] Swagger 已通过 Nginx IP 白名单限制
- [ ] 前端 `dist` 已部署，浏览器访问首页 200 OK
- [ ] 默认 `admin` 账号已完成首次改密

### 8.3 数据 / 备份

- [ ] MySQL / MinIO 数据目录在独立磁盘或定期备份的目录
- [ ] 已配置 [§9 备份脚本](#9-备份与恢复) 并通过 cron 验证 1 次

### 8.4 监控

- [ ] `/actuator/health` 接入主机监控（Prometheus / Zabbix / 阿里云 ARMS / 七牛监控等）
- [ ] Nginx access/error 日志接入日志平台
- [ ] 磁盘使用率告警阈值 80%
- [ ] 后端日志收集（`logs/app.log` 滚动 30 天）

### 8.5 安全

- [ ] HTTPS 证书有效期 > 60 天，已配置自动续期
- [ ] Nginx 已配置安全响应头（HSTS、X-Content-Type-Options 等）
- [ ] 已建立至少 1 名应急联系人 + 临时备用管理员账号

---

## 9. 备份与恢复

> 完整脚本待 [IT-4.4](./后续迭代计划.md) 沉淀到 `ops/backup.sh`，下文为可立刻使用的基础版。

### 9.1 MySQL 全量备份

```bash
# /opt/contract-mgmt/scripts/backup-mysql.sh
#!/bin/bash
set -euo pipefail
DATE=$(date +%F)
BACKUP_DIR=/opt/contract-mgmt/backup/mysql
mkdir -p "$BACKUP_DIR"
docker exec cm-mysql-dev sh -c \
  "exec mysqldump --single-transaction --quick --routines --triggers \
   -uroot -p\"\$MYSQL_ROOT_PASSWORD\" contract_mgmt" \
  | gzip > "$BACKUP_DIR/contract_mgmt-$DATE.sql.gz"
find "$BACKUP_DIR" -name '*.sql.gz' -mtime +30 -delete
```

cron（每天 02:00）：

```cron
0 2 * * * /opt/contract-mgmt/scripts/backup-mysql.sh >> /opt/contract-mgmt/logs/backup.log 2>&1
```

### 9.2 MinIO 备份

方案一（推荐）：使用 `mc mirror` 同步到异机或对象存储

```bash
mc alias set local http://127.0.0.1:9000 $MINIO_ACCESS_KEY $MINIO_SECRET_KEY
mc alias set remote http://backup-host:9000 ... ...
mc mirror --overwrite local/contract-mgmt remote/contract-mgmt-bk
```

方案二：直接打包 `/opt/contract-mgmt/data/minio` 目录（停服窗口期）。

### 9.3 数据恢复演练

每季度至少演练 1 次恢复流程：

```bash
# MySQL：还原最新备份到测试库
gunzip -c contract_mgmt-2026-05-12.sql.gz \
  | docker exec -i cm-mysql-dev mysql -uroot -p"$MYSQL_ROOT_PASSWORD" contract_mgmt_restore

# MinIO：mirror 反向同步或解压 tar 到新挂载目录
```

恢复后用对账查询验证：

```sql
SELECT COUNT(*) FROM contract;
SELECT COUNT(*), SUM(amount) FROM payment;
```

---

## 10. 升级与回滚

### 10.1 升级前

- [ ] 备份 MySQL + MinIO（必做）
- [ ] 在测试环境完整跑通新版本
- [ ] 评估 Flyway 是否有新增 `Vxxx__*.sql`；如有，记录变更范围
- [ ] 准备回滚版本的 jar / 镜像

### 10.2 升级步骤（后端）

```bash
# 1. 备份当前 jar
mv /opt/contract-mgmt/app/contract-mgmt-backend.jar \
   /opt/contract-mgmt/app/contract-mgmt-backend-$(date +%Y%m%d).jar.bak

# 2. 部署新 jar
cp contract-mgmt-backend.jar /opt/contract-mgmt/app/

# 3. 平滑重启
sudo systemctl restart contract-mgmt-backend

# 4. 健康检查
sleep 15 && curl -sf http://127.0.0.1:8080/actuator/health
```

### 10.3 升级步骤（前端）

```bash
# 1. 备份当前 dist
sudo mv /var/www/contract-mgmt /var/www/contract-mgmt.bak-$(date +%Y%m%d)
sudo mkdir /var/www/contract-mgmt
sudo cp -r frontend/dist/* /var/www/contract-mgmt/
sudo chown -R www-data: /var/www/contract-mgmt

# 2. 强制刷新缓存（让用户拉新版）
# 前端构建已带 hash，无需额外动作；Nginx 不需要 reload
```

### 10.4 回滚

```bash
# 后端回滚
sudo systemctl stop contract-mgmt-backend
mv /opt/contract-mgmt/app/contract-mgmt-backend.jar /tmp/
mv /opt/contract-mgmt/app/contract-mgmt-backend-20260512.jar.bak \
   /opt/contract-mgmt/app/contract-mgmt-backend.jar
sudo systemctl start contract-mgmt-backend

# 前端回滚
sudo rm -rf /var/www/contract-mgmt
sudo mv /var/www/contract-mgmt.bak-20260512 /var/www/contract-mgmt
```

> **Flyway 不支持自动 downgrade**。若新版本含破坏性 DDL（删字段、改类型），回滚必须先从备份还原数据库，再回退应用版本。

---

## 11. 监控、日志与告警

### 11.1 健康检查端点

| 路径 | 用途 | 鉴权 |
| --- | --- | --- |
| `/actuator/health` | 整体健康 | 否（可被监控直接探测） |
| `/actuator/info` | 版本信息 | 否 |
| `/actuator/metrics` | JVM 指标 | 仅内网可访问（IT-1.7 后增强为鉴权访问） |
| `/swagger-ui.html` | 接口文档 | 仅内网 |

### 11.2 日志位置

| 来源 | 路径 | 滚动 |
| --- | --- | --- |
| 后端应用日志 | `/opt/contract-mgmt/logs/app.log` | 单文件 100MB / 保留 30 天 |
| systemd 标准输出 | `/opt/contract-mgmt/logs/backend.log` | 由 systemd 追加，建议 logrotate |
| Nginx 访问日志 | `/var/log/nginx/contract-access.log` | logrotate 默认 |
| MySQL 慢查询 | 容器 `/var/lib/mysql/slow.log` | 需在 my.cnf 配置 |

### 11.3 告警建议

| 告警项 | 阈值 | 处理 |
| --- | --- | --- |
| `/actuator/health` 连续 3 次非 UP | - | 立刻拉群，看 backend.log |
| 后端日志 `ERROR` 计数 > 10/min | - | 看是否雪崩，必要时回滚 |
| MySQL 连接数 > 80% | maxConnections=200 | 排查慢 SQL / 连接泄漏 |
| 磁盘使用率 > 80% | - | 触发清理 / 扩容流程 |
| MinIO 桶使用率 > 80% | 由 `storage.alert_threshold_percent` 参数控制 | 联系业务清理或扩容 |
| HTTPS 证书过期 < 30 天 | - | 立刻续期 |

---

## 12. 安全加固

### 12.1 操作系统

- 禁用 root SSH 登录，仅允许密钥
- 安装 `fail2ban` 防爆破
- `ufw` / `firewalld` 只开 80、443、22（22 限源 IP）

### 12.2 应用层

- `JWT_SECRET` ≥ 64 位随机串，**严禁** 使用 `please_change_me_*` 默认值（启动会产生告警）
- 生产 `management.endpoint.health.show-details: never`
- Swagger 通过 Nginx IP 白名单或下线（生产可在打包时排除 springdoc）
- 强制 HTTPS、HSTS 一年
- 启用 [IT-1.7 限流硬化](./后续迭代计划.md)（登录接口 5 次/分钟、敏感写接口 60 次/分钟）

### 12.3 数据库

- 应用账号最小权限（仅 SELECT / INSERT / UPDATE / DELETE / EXECUTE，不允许 DROP / ALTER）
- 数据库网络仅绑定 `127.0.0.1` 或专属内网
- 定期巡检 `audit_log` 表

### 12.4 对象存储

- MinIO bucket 默认 private；前端访问通过后端签发的预签名 URL，TTL 15 分钟（`app.minio.presigned-expire-seconds`）
- 9001 控制台仅允许内网

---

## 13. 性能调优

### 13.1 后端 JVM

| Heap < 4G | Heap 4-8G | Heap > 8G |
| --- | --- | --- |
| `-XX:+UseG1GC -XX:MaxGCPauseMillis=200` | `-XX:+UseG1GC -XX:MaxGCPauseMillis=150 -XX:InitiatingHeapOccupancyPercent=45` | 考虑切换 ZGC（JDK 17 已 GA） |

### 13.2 数据库

| 索引建议 | 已建 |
| --- | --- |
| `contract(owner_id, sign_date)` | ✅ `idx_owner` |
| `contract(status, expire_date)` | ✅ `idx_status` |
| `payment_plan_item(contract_id, period_no)` | ✅ `uk_contract_period` |
| `payment(contract_id, received_date)` | ✅ `idx_contract` |
| `audit_log(user_id, created_at)` | ✅ `idx_user_time` |

`my.cnf` 关键参数：

```ini
innodb_buffer_pool_size = 物理内存 * 50%
innodb_flush_log_at_trx_commit = 1   # 强一致；非金融场景可调 2
slow_query_log = 1
long_query_time = 1
```

### 13.3 前端

- 已开启路由懒加载（每个 view 一个 chunk）
- 静态资源 Nginx `Cache-Control: public, immutable` + 30 天
- gzip / brotli 由 Nginx 提供（建议同时打开）

---

## 14. 故障排查手册

> README 也有简表，下面是扩展版。

### 14.1 启动期

| 现象 | 根因 | 处理 |
| --- | --- | --- |
| `couldn't find env file: .env` | 缺 `.env` | `cp .env.example .env` |
| `./mvnw: no such file or directory` | Maven Wrapper 缺失 | 在 `backend/` 执行 `mvn -N wrapper:wrapper -Dmaven=3.9.6` |
| `Access denied for user 'root'@'xxx'` | 宿主机 3306 被其它项目的 MySQL 占用，导致 Spring Boot 连到了错的库 | `docker ps -a` 找到冲突容器，停掉；再 `docker compose -f docker-compose.dev.yml --env-file .env down && up -d` |
| `Public Key Retrieval is not allowed` | JDBC URL 缺 `allowPublicKeyRetrieval=true` | 已在示例配置中默认开启，检查是否覆盖 |
| `Web server failed to start. Port 8080 was already in use` | 8080 被其它进程占用 | `lsof -nP -iTCP:8080 -sTCP:LISTEN`、`kill <PID>` |
| Flyway `Migration checksum mismatch` | 已应用脚本被修改 | 开发期：删表后重启；生产期：新增 `V2__patch.sql`，不要改 V1 |
| 启动后 `MinIO connection refused` | MinIO 没起 / 网络隔离 | `docker logs cm-minio-dev`；检查 `app.minio.endpoint` |

### 14.2 运行期

| 现象 | 根因 | 处理 |
| --- | --- | --- |
| 登录提示"账号已锁定" | 连续 5 次密码错 | 1) 等 10 分钟；2) 紧急解锁：进 Redis `DEL login:lock:<username> login:fail:<username>` |
| 登录提示"用户名或密码错误"但密码正确 | `password_hash` 异常 / 区分大小写 | `SELECT password_hash FROM sys_user WHERE username='admin'`，确认是 `$2b$10$...`；可重置回默认 hash |
| 改密后没跳首页 | 前端未刷新 `mustChangePwd` 状态 | 已修复（V1.0），若复现：清浏览器缓存或检查 `/auth/me` 返回 |
| 大量 401 | JWT 过期或被加入黑名单 | 让用户重新登录；如批量出现，检查 Redis 是否被清空（黑名单丢失） |
| 合同编号断号 | 跨天 / Redis 重启 / 切换实例 | 后端启动会从 DB 校对 max(serial)；同一年内偶发断号属正常 |
| 回款核销异常（金额不一致） | 并发或人工改库 | 1) 暂停回款写入；2) 用 `SELECT FOR UPDATE` 锁合同；3) 重算 `contract.paid_amount = SUM(payment.amount)`；4) 重算 `payment_plan_item.received_amount` |

### 14.3 性能

| 现象 | 排查 |
| --- | --- |
| API 响应慢 | `actuator/metrics` 看 `http.server.requests`；定位慢 SQL：`SHOW PROCESSLIST` + `slow_query_log` |
| 内存涨 | `jcmd <pid> VM.native_memory summary`、看 `app.log` 中的 GC 日志 |
| Redis 满 | `INFO memory`；按需把 `audit_log` 异步队列改成 DB 直写 |

### 14.4 数据

| 现象 | 处理 |
| --- | --- |
| 用户误删合同（软删） | `UPDATE contract SET deleted=0 WHERE id=?`（仅 DBA） |
| MySQL 损坏 | 从最近备份恢复（[§9.3](#93-数据恢复演练)） |
| MinIO 桶丢文件 | 从 mirror 副本恢复；同步 audit_log 排查删除来源 |

---

## 15. 附录：常用运维命令速查

```bash
# 容器状态
docker compose -f docker-compose.prod.yml --env-file config/.env ps
docker stats --no-stream

# 进容器
docker exec -it cm-mysql-dev mysql -uroot -p$DB_ROOT_PASSWORD contract_mgmt
docker exec -it cm-redis-dev redis-cli -a $REDIS_PASSWORD

# 后端服务
sudo systemctl status contract-mgmt-backend
sudo journalctl -u contract-mgmt-backend -f --since "10 min ago"
tail -f /opt/contract-mgmt/logs/app.log

# Nginx
sudo nginx -t                                       # 配置语法
sudo systemctl reload nginx
tail -f /var/log/nginx/contract-access.log

# 数据库快速查询
mysql -h127.0.0.1 -uroot -p$DB_ROOT_PASSWORD contract_mgmt -e \
  "SELECT status, COUNT(*) FROM contract GROUP BY status;"

# 清理 Redis（谨慎！会强制所有用户重新登录）
docker exec -it cm-redis-dev redis-cli -a $REDIS_PASSWORD KEYS 'login:*' | xargs -I{} \
  docker exec cm-redis-dev redis-cli -a $REDIS_PASSWORD DEL {}

# 验证 JWT 黑名单大小
docker exec -it cm-redis-dev redis-cli -a $REDIS_PASSWORD DBSIZE
```

---

> 文档归口：DevOps 团队。配合 [系统操作手册.md](./系统操作手册.md) 阅读。
> 反馈与勘误：在 Issue Tracker 中提 issue，加标签 `docs/deploy`。
